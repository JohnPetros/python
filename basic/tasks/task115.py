# task 115 - Create a small modularized module that allows for registering users by their name and age into a simple text file. The system will only contain two options: register a new user and list all registered users.

from packages.utils import registration

registration.show_menu()
